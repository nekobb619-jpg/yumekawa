# AGENTS.md — 「おしえて！ラボ」コア開発指示書（Antigravity CLI / Claude Cowork共用）

本ファイルは Antigravity CLI および Claude (Cowork) が作業前に自動読み込みする最重要ルールブックです。
`CLAUDE.md`は作らず、このファイル1本に指示を集約しています（どちらのAIで作業しても同じ前提に
なるようにするため）。詳細な仕様・過去の経緯は、必要に応じて `.knowledge/` 内のファイルを
指定して読み込んでください（毎回全部読む必要はありません＝トークン節約）。

---

## 1. プロジェクト概要 & ペルソナ
- **目的**：小学4年生向け総合学習アプリ「おしえて！ラボ」（本番アプリ名「ゆめかわ★学びアドベンチャー
  LAB Pro」、GitHub Pages + Google Apps Script/Sheetsバックエンド、リポジトリ`nekobb619-jpg/yumekawa`）。
  AIの友達キャラと会話しながら体験・実験・講義で学び、「友達に教える」→「問題をつくる」を通して
  **問題のねらい（意図）を見ぬく力** を育てる。問題は4年生の単元を優先、途中で3年生までの
  前提スキルを診断して穴うめ（スパイラル方式）。
- **ターゲット**：小学4年生（ひらがな・ふりがな前提、大きめのUI、やさしい相棒口調）。
- **キャラ**：ニコ（相棒・進行役）、クロぽん（ごほうび・息抜き）。詳細は `.knowledge/persona-yumekawa.md`。

## 2. アーキテクチャ基本原則（1ファイル方式）
- **フロント**：素の HTML/CSS/JS（`index.html` 1ファイル中心、ビルドツールなし、GitHub Pages配信）。
  ラボ（体験・実験パート）は `lab/教科/xxx.html` の個別ファイル。
- **データ一元化**：全ステージ（`STAGES`）は `js/stages.js`、全問題（`QUIZZES`）は教科ごとに分割した
  `js/quizzes/math.js`（算数）・`science.js`（理科）・`japanese.js`（国語）・`social.js`（社会）・
  `kanji.js`（漢検）・`inquiry.js`（探究）・`english.js`（英語）で、ロジック（`stableQid`/
  `migrateWeakQuestionIds`/`injectContentStages`等・自動パッチ処理）は `js/patch.js` で管理する
  （2026-07-29、旧`content.js`から分割 → 同日中に`quizzes.js`をさらに教科別へ再分割。
  AI編集コスト・トークン消費削減が目的。ファイル名は日本語教科名だとOS間トラブルの懸念があるため
  英語表記）。`index.html`では `js/stages.js` → `js/quizzes/*.js`（各教科） → `js/patch.js` の順で
  読み込むこと（`patch.js`が `window.CONTENT_STAGES` と教科ごとの `window.CONTENT_QUIZZES_*`
  （`_MATH`/`_SCIENCE`/`_JAPANESE`/`_SOCIAL`/`_KANJI`/`_INQUIRY`/`_ENGLISH`）を合流させるため、
  この順番が必須）。スプレッドシート側に行を足す必要はない。データ保存はlocalStorage＋GAS経由で
  スプレッドシートにも保存。
- **台帳同期**：新ステージの追加・変更時は `問題管理台帳.html` の `LEDGER` 配列も**必ず同時更新**すること。
- 実装の詳しい仕様（データ書式、`js/stages.js`・`js/quizzes/*.js`・`js/patch.js`の各仕組み、
  既存機能の内部動作）は `.knowledge/architecture-content.js.md` を参照。

## 3. クリティカル・ルール（最優先・事故防止）
過去に実際の事故につながった、破ると即トラブルになるルール。詳しい経緯は `.knowledge/postmortems.md`。
1. **保存・ログ送信の先行完了**：問題セット終了時、復習チャットや新しい割り込みモーダルより**前に必ず**
   `exitToMainMenu` を呼び出し、データの保存・ログ送信を完了させてから次の画面へ分岐すること。
2. **セーブデータ空上書きガード**：`window.saveDataConfirmedSource` が `null`（＝本物のデータを
   一度も確認できていない）の状態では、クラウドへの `SAVE` 通信を絶対に行わないこと。
3. **苦手リストの安定識別**：苦手問題は `ステージid::qid`（安定ハッシュID）形式で保存すること。
   新しい問題タイプを追加するときも`triggerWrongAnswer`をちゃんと呼ぶこと。
4. **台帳同期**：ステージを追加・変更したら `問題管理台帳.html` の `LEDGER` も同じタイミングで更新する。
5. **納品前の機械的検証**：`js/stages.js`/`js/quizzes/*.js`/`js/patch.js`/`index.html` の変更後は、Node.jsでの構文・データ検証、
   できればPlaywrightでの実ブラウザ確認を完了してから納品すること。手順は `.knowledge/guide-testing.md`。

## 3.5 問題生成時の内容精査チェックリスト（必須）
問題（`QUIZZES` エントリ）を新規追加・修正するときは、コードへの書き込み**前**に以下を必ず自己チェックすること。
1つでも「×」があれば修正してから追加すること。

### ✅ 内容の正確性
- [ ] **正答が実際に正しいか**：教科書レベルの知識で裏付けができるか確認する。「たぶん合ってる」で済ませない。
- [ ] **誤答選択肢が明確に誤りか**：正答と混同されうる紛らわしい誤答を意図的に作る場合も、
      それが学習上の効果的な引っかけであり、正答と誤答の区別が実際に明確か確認する。
- [ ] **学習指導要領との対応**：当該問題が対象学年（4年生、または診断用の3年生以下）の
      単元・内容に対応しているか確認する。学年超過や範囲外は明記してから追加すること。

### ✅ 表現・文言
- [ ] **問題文がひらがな・ふりがなで読める**：小学4年生が単独で読める表現か。
      難読漢字にはふりがなを付ける（ただし**答えになる漢字の読みをふりがなで漏らさない**こと）。
- [ ] **指示文が一意に解釈できる**：「次の□に入る言葉を選べ」等、何を答えるか一読で分かるか。
- [ ] **選択肢の表記統一**：同じ問題内の選択肢で表記ゆれ（漢字・ひらがな混在等）がないか。

### ✅ データ整合性
- [ ] **`qid` がステージ内で一意**：同じステージ内に同じ `qid` が存在しないか。
- [ ] **`correctIndex` と `choices` の配列長が一致**：`choices[correctIndex]` が正答に対応しているか。
- [ ] **`hint` / `explanation` の有無**：苦手フォロー用に `hint` または `explanation` を設定したか。
- [ ] **`triggerWrongAnswer` の呼び出し**：新しい問題タイプを追加した場合、不正解時に
      `triggerWrongAnswer` が正しく呼ばれるか確認する（クリティカルルール3参照）。
- [ ] **`text_input`の`correct_answers`は自然な表記ゆれをすべて含める**：特に「漢字＋送り仮名」の
      読みを書かせる問題（例：「必ず」→ `かならず`）は、送り仮名を含めた読み（`かならず`）だけでなく、
      漢字部分だけの読み（`かなら`）も正解として許容すること。同様に、ひらがな/カタカナ/漢字の
      どれで書いても意味が同じ場合はすべて列挙する（既存の良い例：`["じゅつご","述語"]`）。
      1つの表記しか登録しないと、子どもが自然に書いた別表記が不正解扱いになる
      （2026-08-04、『必ず』の読み問題で発覚。`.knowledge/postmortems.md`参照）。

### ✅ 教育的妥当性
- [ ] **難易度がターゲット学年に合っているか**：簡単すぎ・難しすぎがないか。
- [ ] **問題の「ねらい」が明確か**：この問題で何の概念・スキルを測るか説明できるか。
- [ ] **既存問題との重複がないか**：同じステージ内に実質同じ問題が存在しないか確認する。

> ⚠️ **AIが「たぶん正しい」と思っても、確認なしに追加しない。**
> 事実確認が必要な場合は、ユーザーに確認を取るか、信頼できる情報源（教科書・学習指導要領）を
> 参照してから追加すること。不確かな問題は「要確認」コメントを付けてユーザーに提示する。

## 3.6 思考力・推論問題の設計方針（2026-08-03 追加）
暗記・単純計算だけでなく「条件整理」「法則性発見」「複数ステップの思考」を要する
問題（`thinking:true`フラグ付き）を、通常ステージ・探偵ラボの両方に意図的に混在させる。

- **通常ステージ**：`js/quizzes/*.js`の各単元配列に、`thinking:true`を付けた問題を
  データとして追加する。抽出（`launchQuest`）は`window.pickBalancedQuestions(bundle, showCount)`
  を通すことで、`showCount < 全問題数`の単元では`thinking:true`の問題が最低1問は
  出題に含まれるようになる（保証は最低1問のみ。全問`thinking`にはしない＝基礎の反復練習と
  思考力問題のバランスを保つ）。
- **探偵ラボ**：`window.DYNAMIC_GENERATORS[subject].normal`に教科ごとの思考力ジェネレーター
  （`genMathThinkingCondition`/`genKokugoThinkingLogic`/`genScienceThinkingCondition`/
  `genShakaiThinkingClues`）を追加。`startDetectiveLab()`は実問題4問＋思考力ジェネレーター1問
  の計5問構成にすることで、毎回必ず1問は思考力問題が出るようにしている
  （6・7問目のボス「ニコの探偵ノート」も逆推論形式＝広い意味で思考力枠）。
- **新しい思考力問題を追加するとき**：正誤が一意に定まる（ロジックパズルなら唯一解になる）
  ことを必ず確認する（3.5の内容精査チェックリストと同様に扱う）。曖昧な複数正解が生まれる
  設問は不可（2026-08-03、国語の順位推理クイズで「ヒント2つを組み合わせても3人目の順位が
  決まらない」曖昧なケースが実際に見つかり、ヒントを必ず順位で隣どうしの2人を比較する形に
  固定して解消した。新しいロジックパズルを作るときはこの教訓を踏まえること）。

## 4. 絶対禁止事項（Prohibitions）
- **APIキーの露出**：Claude/Gemini 等の API キーをフロントエンド（ブラウザ側）に置かないこと
  （サーバー側プロキシ必須）。
- **問題文での答え漏れ**：漢字問題等の読み問題本文（`q`）にふりがなを書かないこと
  （詳細は `.knowledge/roadmap-kanken.md`）。
- **TTS音声での答え漏れ**：`speech_text`に、隠している正解（読み・語句）そのものを含めないこと
  （読ませたい対象は`q`で画面表示するのみに留め、`speech_text`は定型的な問いかけ文にする。
  2026-08-03、探偵ラボの漢字読み問題でTTSが答えを直読みしてしまうバグが実際に発生した）。
- **無断改変・削除**：ユーザーの明示的な依頼なしに GAS（`コード.gs`）やレガシーコード
  （個別 `questions.js` 等）を触らないこと。
- **キャラの逸脱**：こわい／きずつく／大人向けの話題を出さないこと。話がそれたらやさしく学習に戻す。
  個人情報（名前・住所・学校名など）は聞かない・保存しない。

## 5. ナレッジファイルの参照指示
タスクに応じて、以下のファイルを明示的に読み込んで作業すること。
- `js/stages.js`・`js/quizzes/*.js`・`js/patch.js` の詳細構造・既存機能の内部動作・新ステージ追加のパターン ➔ `.knowledge/architecture-content.js.md`
- 苦手分野のログ分析・Gemini APIでの問題自動生成（`js/adaptive-question-generator.js`） ➔ `.knowledge/architecture-content.js.md` 13.
- 過去の重大バグ（セーブ消失・保存スキップ等）の背景・教訓 ➔ `.knowledge/postmortems.md`
- 漢検10級コンテンツの作成・追加 ➔ `.knowledge/roadmap-kanken.md`
- 検証コード・Playwrightテストの実行手順 ➔ `.knowledge/guide-testing.md`
- キャラセリフ・文言ルール・アクセシビリティ ➔ `.knowledge/persona-yumekawa.md`

## 6. 複数AI併用について（Claude Cowork × Antigravity CLI）
- このリポジトリは Claude（Cowork）と Antigravity CLI の両方で並行開発される想定。
- **同時に同じファイルを2つのAIで編集しない**こと。片方が作業→コミット→push→もう片方が
  `git pull` してから作業、の順番を守る。
- 新しい設計判断（アーキテクチャ変更、命名規則の変更、新しい絶対ルールなど）をしたら、
  このファイル（と該当する`.knowledge/*.md`）に反映して、次にどちらのAIが読んでも
  同じ前提で作業できるようにすること。
- **（2026-07-26追加・必須）`device_commit_files`でファイルを書き戻す直前に、必ず
  `device_bash`で対象ファイルの`git log --oneline -5`と`git status -sb`を確認し、
  自分が最後に読み込んだ内容から進んでいないか毎回チェックする。もう一方のAIが
  ユーザーの端末で直接ファイルを編集・コミットしている可能性があるため（詳細は
  `.knowledge/postmortems.md`の2026-07-26の事故未遂を参照）。

## 7. ディレクトリ構成・やらないこと
- 詳しいディレクトリ構成は `.knowledge/architecture-content.js.md` 末尾を参照。
- **やらないこと（今回）**：本物の友達とのオンライン機能／課金／先生用の管理画面（将来）、
  星座マッチング（「ニコ一本」でシンプルに）、GASの仕様変更（依頼なしに触らない）、
  過去の個別`questions.js`やレガシーラボファイルの削除・整理（依頼があるまで現状維持）。

---
*不明点は勝手に決めず、`docs/`（初期設計の要件定義・デザイン方針）を確認 → それでも不明なら小さく作って質問する。*
